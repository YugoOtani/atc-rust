mod algo {
    mod binary_search;
    mod combination;
    mod gcd;
    mod graph;
    mod list;
    mod pow;
    mod prime;
}
mod data_structure {
    mod acm2;
    mod bit;
    mod btreeset;
    mod segment_tree;
    mod uf;
}
mod types {
    mod frac;
    mod modint;
}
mod cheet_sheet;

// 本来はモジュール名と同名のファイル(alog.rs)を作り、
// その中にmod宣言を書くのが一般的らしい
